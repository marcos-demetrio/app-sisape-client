(function () {
	'use strict';

	angular
		.module('app')
		.controller('InicioController', InicioController);

	InicioController.$inject = ['$scope', '$location', '$window'];

	function InicioController($scope, $location, $window) {

	}
})();
